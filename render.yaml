services:
  - type: web
    name: signara-ml-api
    runtime: python
    rootDir: sign_ai
    buildCommand: pip install -r requirements_api.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    plan: starter
